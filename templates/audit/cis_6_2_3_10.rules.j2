## CIS Ubuntu 26.04 v1.0.0 | 6.2.3.10 | Ensure use of privileged commands are collected
{% for proc_path in cis_ubuntu2604_audit_proc_paths.stdout_lines | sort %}
-a always,exit -F arch=b64 -S all -F path={{ proc_path }} -F perm=x -F auid>={{ cis_ubuntu2604_audit_uid_min }} -F auid!=unset -k privileged
{% endfor %}
